# Generado por LangGraph + Qwen Local
# Prompt: "¿Qué es una función en Python?"
# Modelo clasificador: qwen-2.5
# Modelo ejecutor: qwen-concept-7b
# Fecha: 2026-04-25 21:00:04
# Switch Llama: False

Una función en Python es un bloque de código reutilizable que realiza una tarea específica. Permite organizar el código de manera más limpia y facilita la reutilización del mismo para diferentes partes del programa o incluso en otros programas.

Características principales:
1. Define una serie de instrucciones.
2. Puede recibir entradas (argumentos).
3. Puede devolver un resultado.
4. Aumenta la legibilidad y mantenibilidad del código.

Ejemplo básico:

```python
def saludar(nombre):
    return f"Hola, {nombre}!"

print(saludar("Juan"))
```

En este ejemplo, `saludar` es una función que toma un nombre como argumento y devuelve una saludo personalizado.